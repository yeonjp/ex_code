reset.css 파일 구성요소

@charset "UTF-8"; /* 인코딩으로 인해 발생할 수 있는 문자 깨짐 방지 */

html, body, div, span, applet, object, iframe,
h1, h2, h3, h4, h5, h6, p, a, big, cite, code,
 em, img, ins, q, s, small, strike, strong, sub, sup,
b, u, i, center,
dl, dt, dd, ol, ul, li,
fieldset, form, label, legend,
table, caption, tbody, tfoot, thead, tr, th, td,
article, aside, canvas, details, embed, 
figure, figcaption, footer, header, hgroup, 
menu, nav, output, ruby, section, summary,
time, mark, audio, video {
	margin: 0; /* 테두리와 외부 간격 */
	padding: 0; /* 테두리와 글자 사이의 간격 */
	border: 0; /* 테두리 */
	font-size: 100%; /* 글자 크기 */
	vertical-align: baseline; /* 수직정렬 */
    /*background:transparent; 배경색 투명하게 */
}

body{ /* body 태그에만 적용 */
line-height:1; /* line - height : 줄 간격() */
}

article,aside,details,figcaption,figure,footer,header,hgroup,menu,nav,section{ 
display:block;/* display : 너비, 높이를 가질 수 있게, 값으로 block, inline을 가짐. */
}

ol, ul, li{
list-style:none; /* list - style을 없앰. ol : 순서 있음, ul : 순서 없음, li : 항상 ol, ul과 함게 사용해야 함. */
}

a{ /* 링크 걸 때 사용하는 태그 */
margin:0;
padding:0;
font-size:100%;
text-decoration:none; /* text decoration : 밑줄 */
vertical-align:baseline;
color:#000000; /* 글자색 */
background:transparent; /* 배경색 투명 */
}

img{
vertical-align:top; /* 이미지 사이의 간격을 없앰 */
}

table{ /* 표를 만들 때 사용함. 테이블의 테두리 사이 간격 없앰 */
border-collapse:collapse;  
border-spacing:0;
}


